apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Fixed uid so the provisioned dashboard can bind to it without an import
    # prompt (the dashboard JSON references uid "prometheus").
    uid: prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: false
